#include <stdio.h>
int a, b, x;

int main()

{
    printf("enter 1 st number ....:");

    scanf("%d", a);
    printf("enter 2nd number ...:");
    scanf("%d", b);

    x = a;
    a = b;
    b = x;
    printf("swip value is ..:%d ,%d", a, b);

    return 0;
}
